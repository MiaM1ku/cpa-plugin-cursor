# Cursor plugin v0.1.0

- Expose base Cursor models only; pass reasoning with OpenAI `reasoning_effort`.
- Inject Cursor dashboard session cookies for CPA Manager Plus quota refresh via `executor.http_request`.
- Report OpenAI usage from Cursor `token_delta` when present; do not register a usage observer plugin, so CPA usage analytics is not double-counted.
- No separate plugin quota page.
