# Cursor plugin v0.1.4

- Treat `-max` like `-1m` as Max mode, and keep stripping effort suffixes until only the base model remains.
- Collapse families such as `claude-fable-5`, `...-thinking`, `...-thinking-high`, `...-max`, and `...-thinking-max` to one disable-list entry.
