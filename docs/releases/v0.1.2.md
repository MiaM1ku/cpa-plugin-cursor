# Cursor plugin v0.1.2

- Translate Codex WHAM usage into Cursor dashboard percentages as Codex primary/weekly windows so CPA Manager Plus quota cards can display them.
- Register `quota.provider` and a Cursor 额度 management module.
- Reset-credits probes return an empty list instead of failing the Codex quota refresh path.
