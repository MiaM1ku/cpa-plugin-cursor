# Cursor plugin v0.1.1

- Keep v0.1.0 behavior: base models plus `reasoning_effort`, dashboard session quota passthrough, and single-shot OpenAI usage.
- Fix GitHub Actions packaging: store `packaging/*.sh` as executable and invoke them with `sh`.
